# Frontend-Admin

Admin-facing HappyTails React app.

## Run

1. `npm install`
2. Create `.env` from `.env.example`
3. `npm run dev`

## Required environment variables

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_API_BASE_URL` defaults to `https://pethub-customer-api.onrender.com/api`
