# Frontend-Customer

Customer-facing HappyTails React app.

## Run

1. `npm install`
2. Create `.env` from `.env.example`
3. `npm run dev`

## Required environment variables

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_API_URL` if you later attach a separate customer API
