# Backend-Admin

Admin Express API recovered from the backup admin project.

## Run

1. `npm install`
2. Create `.env` from `.env.example`
3. `npm run dev`

## Required environment variables

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `PORT` optional, defaults to `4000`
